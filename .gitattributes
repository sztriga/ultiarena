models/**/*.pt filter=lfs diff=lfs merge=lfs -text
# Only the .pt weights are LFS. The isotonic .npz tables and snapszer's pkl/json
# are small and stored as ordinary blobs — these attributes must MATCH what is in
# the object store, or a renormalize silently rewrites history-visible content.
