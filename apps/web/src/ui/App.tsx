import { PlayVsAI } from "./PlayVsAI";

// Ulti-first app: boots straight into the PlayVsAI splash (Új játék + Villámtalon).
// No tab nav — this is a single-purpose Ulti web app.
export function App() {
  return <PlayVsAI />;
}
