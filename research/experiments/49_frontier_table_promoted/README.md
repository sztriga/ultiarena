# exp49 — frontier table after the 2026-08-11 promotion

The exp44 harness, unchanged, re-run because BOTH epochs rolled at once:
the suit re-encode (seeds deal different hands) and the pipeline-retrained
bidding heads (all 7, commit e184536). The exp44 corpus is the old world;
this directory is the new one. Looking for bleeders on the full ladder.
